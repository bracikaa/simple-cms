<% subjects ||= [] %>
<ul>
	<% subjects.each do |subject| %>
	<li>
		<%= subjects.name %>
		<ul>
			<% subject.pages.visible.sorted do |page| %>
			<li><%= link_to_unless_current(page.name, public_show_path(page.permalink)) %></li>
			<% end %>
		</ul>
	</li>
	<% end %>
</ul>